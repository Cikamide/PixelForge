<Window x:Class="PixelForge.ClientPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ClientPage" Height="800" Width="1500"
        ResizeMode="CanResize" Background="#0B1120">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Bara de sus -->
            <RowDefinition Height="*"/>
            <!-- Conținut principal -->
        </Grid.RowDefinitions>

        <!-- Bara de sus cu buton inapoi/delogare -->
        <Grid Background="#1E293B" Height="60">
            <Button Content="⟵ Înapoi"
                    Click="LogoutButton_Click"
                    Background="#334155"
                    Foreground="White"
                    FontSize="16"
                    FontWeight="Bold"
                    Padding="10,5"
                    Margin="10"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Cursor="Hand"/>
        </Grid>

        <!-- Conținutul principal -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
            <TextBlock Text="Panou Client"
                       FontSize="36"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,40" />

            <Button Content="Creare PC"
                    Click="CrearePCButton_Click"
                    Background="#3B82F6"
                    Foreground="White"
                    FontSize="20"
                    FontWeight="Bold"
                    Height="60"
                    Margin="0,10"
                    HorizontalAlignment="Stretch" />

            <Button Content="Status Comenzi"
                    Click="StatusComenziButton_Click"
                    Background="#3B82F6"
                    Foreground="White"
                    FontSize="20"
                    FontWeight="Bold"
                    Height="60"
                    Margin="0,10"
                    HorizontalAlignment="Stretch" />
        </StackPanel>
    </Grid>
</Window>
